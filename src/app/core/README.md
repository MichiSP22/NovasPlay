# Core

Codigo transversal y singleton de Angular:

- `http`: cliente API base.
- `auth`: autenticacion, sesion y guards.
- `interceptors`: interceptores HTTP.
- `platform`: utilidades de navegador, SSR y responsive.

No colocar componentes de pantalla aqui.
