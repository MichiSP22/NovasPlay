# Core Auth

Autenticacion global:

- servicio de sesion
- login/logout
- refresh
- guards
- modelos de auth compartidos por la app

Los formularios visuales de login o register deben vivir en `features/auth`.
