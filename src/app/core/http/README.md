# Core HTTP

Cliente HTTP base, configuracion de headers, `withCredentials`, manejo de `environment.apiUrl` y wrappers genericos.

Los servicios de dominio no deben construir URLs base por su cuenta; deben apoyarse en esta capa.
