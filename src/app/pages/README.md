# Pages

Componentes conectados directamente a rutas.

Responsabilidades:

- componer layout, features y entidades
- manejar parametros de ruta
- decidir estados de pantalla

Evitar que una page acumule transformaciones grandes de API; mover esas conversiones a `entities/<domain>/mappers`.
