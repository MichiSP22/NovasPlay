# Shared Utils

Funciones puras y helpers sin estado:

- formateo de texto
- normalizacion de URLs
- parseo seguro
- fechas y numeros

Si una utilidad conoce un dominio concreto, debe vivir dentro de `entities/<domain>` o `features/<domain>`.
