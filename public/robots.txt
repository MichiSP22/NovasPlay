User-agent: *
Allow: /

# Private HTML is controlled with meta/X-Robots-Tag noindex so Google can read it.
# We do not block HTML routes here because robots.txt would hide the noindex signal.

Sitemap: https://novasplay.neocharge.app/sitemap.xml
